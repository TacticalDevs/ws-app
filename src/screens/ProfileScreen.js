import { View, Text } from 'react-native';

const ProfileScreen = ({ id }) => {
  console.warn(id);
  return (
    <View>
      <Text>Profile</Text>
    </View>
  );
};

export default ProfileScreen;
